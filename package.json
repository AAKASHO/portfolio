{
  "name": "mern-porfolio",
  "version": "1.0.0",
  "description": "portfolio app",
  "main": "server.js",
  "scripts": {
    "build": "npm install --legacy-peer-deps && npm install --prefix clients --legacy-peer-deps && npm run build --prefix clients --legacy-peer-deps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "techinfoyt",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "netlify-cli": "^17.20.1",
    "netlify-lambda": "^2.0.16",
    "nodemailer": "^6.9.4",
    "nodemailer-sendgrid-transport": "^0.2.0",
    "nodemon": "^3.1.0",
    "serverless-http": "^3.2.0"
  }
}
